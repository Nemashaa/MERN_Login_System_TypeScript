export type Post = {
  postID: number;
  user: string; // User ID as a string
  title: string;
  description: string;
  createdAt: string; // ISO date string
  updatedAt: string; // ISO date string
};
